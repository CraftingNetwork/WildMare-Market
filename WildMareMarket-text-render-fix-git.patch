diff --git a/src/main/java/com/wildmare/market/command/MarketCommand.java b/src/main/java/com/wildmare/market/command/MarketCommand.java
index 55c01ce..cfa8e36 100644
--- a/src/main/java/com/wildmare/market/command/MarketCommand.java
+++ b/src/main/java/com/wildmare/market/command/MarketCommand.java
@@ -116,7 +116,7 @@ public final class MarketCommand implements CommandExecutor, TabCompleter {
             return;
         }
         if (args.length < 3) {
-            usage(player, "/market watchlist &lt;add|remove&gt; &lt;symbol&gt;");
+            usage(player, "/market watchlist \\<add|remove> \\<symbol>");
             return;
         }
         String symbol = args[2].toUpperCase(Locale.ROOT);
@@ -126,7 +126,7 @@ public final class MarketCommand implements CommandExecutor, TabCompleter {
             default -> null;
         };
         if (future == null) {
-            usage(player, "/market watchlist &lt;add|remove&gt; &lt;symbol&gt;");
+            usage(player, "/market watchlist \\<add|remove> \\<symbol>");
             return;
         }
         future.whenComplete((result, throwable) -> sync(() -> {
@@ -156,8 +156,8 @@ public final class MarketCommand implements CommandExecutor, TabCompleter {
         String action = args[1].toLowerCase(Locale.ROOT);
         if (action.equals("add")) {
             if (args.length < 5) {
-                usage(player, "/market alerts add &lt;symbol&gt; "
-                        + "&lt;above|below|percent_up|percent_down|daily_gain|daily_loss&gt; &lt;target&gt;");
+                usage(player, "/market alerts add \\<symbol> "
+                        + "\\<above|below|percent_up|percent_down|daily_gain|daily_loss> \\<target>");
                 return;
             }
             try {
@@ -186,7 +186,7 @@ public final class MarketCommand implements CommandExecutor, TabCompleter {
             return;
         }
         if (args.length < 3) {
-            usage(player, "/market alerts &lt;delete|pause|resume|edit&gt; &lt;alert-id&gt; [target]");
+            usage(player, "/market alerts \\<delete|pause|resume|edit> \\<alert-id> [target]");
             return;
         }
         UUID alertId;
@@ -234,7 +234,7 @@ public final class MarketCommand implements CommandExecutor, TabCompleter {
 
     private void handleSearch(Player player, String[] args) {
         if (args.length < 2) {
-            usage(player, "/market search &lt;symbol or company name&gt;");
+            usage(player, "/market search \\<symbol or company name>");
             return;
         }
         String query = String.join(" ", Arrays.copyOfRange(args, 1, args.length));
@@ -249,7 +249,7 @@ public final class MarketCommand implements CommandExecutor, TabCompleter {
 
     private void handleQuote(Player player, String[] args) {
         if (args.length < 2) {
-            usage(player, "/market quote &lt;symbol&gt;");
+            usage(player, "/market quote \\<symbol>");
             return;
         }
         String symbol = args[1].toUpperCase(Locale.ROOT);
@@ -284,7 +284,7 @@ public final class MarketCommand implements CommandExecutor, TabCompleter {
         if (!allowed(player, "wildmaremarket.trade") || !allowed(player, permission)) return;
         if (args.length < 3) {
             usage(player, "/market " + side.name().toLowerCase(Locale.ROOT)
-                    + " &lt;symbol&gt; &lt;amount&gt;");
+                    + " \\<symbol> \\<amount>");
             return;
         }
         String symbol = args[1].toUpperCase(Locale.ROOT);
diff --git a/src/main/java/com/wildmare/market/util/TextUtil.java b/src/main/java/com/wildmare/market/util/TextUtil.java
index 726bff3..5a5e7f1 100644
--- a/src/main/java/com/wildmare/market/util/TextUtil.java
+++ b/src/main/java/com/wildmare/market/util/TextUtil.java
@@ -33,11 +33,21 @@ public final class TextUtil {
     }
 
     public static Component component(String miniMessage, Map<String, ?> placeholders) {
-        return MINI_MESSAGE.deserialize(replace(miniMessage, placeholders));
+        return MINI_MESSAGE.deserialize(normalizeMiniMessage(replace(miniMessage, placeholders)));
     }
 
     public static Component component(String miniMessage) {
-        return MINI_MESSAGE.deserialize(miniMessage == null ? "" : miniMessage);
+        return MINI_MESSAGE.deserialize(normalizeMiniMessage(miniMessage));
+    }
+
+    /**
+     * Converts legacy HTML-style angle entities used by older configuration files into
+     * MiniMessage-safe literal angle brackets. MiniMessage escapes a literal opening
+     * angle bracket with a backslash, for example {@code \<symbol>}.
+     */
+    static String normalizeMiniMessage(String input) {
+        if (input == null || input.isEmpty()) return "";
+        return input.replace("&lt;", "\\<").replace("&gt;", ">");
     }
 
     public static List<Component> components(List<String> lines, Map<String, ?> placeholders) {
diff --git a/src/main/resources/menus.yml b/src/main/resources/menus.yml
index ad45f3f..d5ac035 100644
--- a/src/main/resources/menus.yml
+++ b/src/main/resources/menus.yml
@@ -23,7 +23,7 @@ main:
     etfs: {slot: 21, material: PAPER, name: "<light_purple><bold>ᴇᴛғs</bold></light_purple>", lore: ["<gray>Browse exchange-traded funds.</gray>"]}
     indexes: {slot: 22, material: COMPASS, name: "<light_purple><bold>ɪɴᴅᴇxᴇs</bold></light_purple>", lore: ["<gray>Browse market indexes.</gray>"]}
     crypto: {slot: 23, material: AMETHYST_SHARD, name: "<light_purple><bold>ᴄʀʏᴘᴛᴏᴄᴜʀʀᴇɴᴄʏ</bold></light_purple>", lore: ["<gray>Browse virtual cryptocurrency assets.</gray>"]}
-    search: {slot: 24, material: SPYGLASS, name: "<light_purple><bold>sᴇᴀʀᴄʜ ᴀssᴇᴛ</bold></light_purple>", lore: ["<gray>Use:</gray> <white>/market search &lt;symbol&gt;</white>"]}
+    search: {slot: 24, material: SPYGLASS, name: "<light_purple><bold>sᴇᴀʀᴄʜ ᴀssᴇᴛ</bold></light_purple>", lore: ["<gray>Use:</gray> <white>/market search \\<symbol></white>"]}
     trending: {slot: 10, material: FIREWORK_STAR, name: "<light_purple><bold>ᴛʀᴇɴᴅɪɴɢ ᴀssᴇᴛs</bold></light_purple>", lore: ["<gray>View assets with notable movement.</gray>"]}
     recent: {slot: 16, material: CLOCK, name: "<light_purple><bold>ʀᴇᴄᴇɴᴛʟʏ ᴛʀᴀᴅᴇᴅ</bold></light_purple>", lore: ["<gray>View the latest globally traded assets.</gray>"]}
     gainers: {slot: 12, material: LIME_DYE, name: "<green><bold>ᴛᴏᴘ ɢᴀɪɴᴇʀs</bold></green>", lore: ["<gray>View the strongest positive changes.</gray>"]}
@@ -98,7 +98,7 @@ quantity:
     slot: 25
     material: NAME_TAG
     name: "<white><bold>ᴄᴜsᴛᴏᴍ ᴀᴍᴏᴜɴᴛ</bold></white>"
-    lore: ["<gray>Use the command:</gray>", "<white>/market {side} {symbol} &lt;amount&gt;</white>"]
+    lore: ["<gray>Use the command:</gray>", "<white>/market {side} {symbol} \\<amount></white>"]
   back-slot: 31
 
 confirmation:
diff --git a/src/main/resources/messages.yml b/src/main/resources/messages.yml
index d19f061..5c4d920 100644
--- a/src/main/resources/messages.yml
+++ b/src/main/resources/messages.yml
@@ -23,8 +23,8 @@ market:
   quote: "<light_purple>{name}</light_purple> <gray>(</gray><white>{symbol}</white><gray>)</gray> <dark_gray>•</dark_gray> <white>{price}</white> <dark_gray>•</dark_gray> {change_color}{change_percent}%</{change_color}> <dark_gray>•</dark_gray> <gray>{age}</gray>"
   open: "<green>Market Open</green>"
   closed: "<red>Market Closed</red>"
-  search-instruction: "<gray>Search with</gray> <white>/market search &lt;symbol or name&gt;</white><gray>.</gray>"
-  custom-amount-instruction: "<gray>Enter a custom amount with</gray> <white>/market {side} {symbol} &lt;amount&gt;</white><gray>.</gray>"
+  search-instruction: "<gray>Search with</gray> <white>/market search \\<symbol or name></white><gray>.</gray>"
+  custom-amount-instruction: "<gray>Enter a custom amount with</gray> <white>/market {side} {symbol} \\<amount></white><gray>.</gray>"
   cache-status: "<gray>Cached Quotes:</gray> <white>{quotes}</white> <dark_gray>•</dark_gray> <gray>Cached History:</gray> <white>{history}</white>"
 
 trade:
@@ -59,7 +59,7 @@ alerts:
   triggered: "<light_purple>Price Alert</light_purple> <dark_gray>•</dark_gray> <white>{symbol}</white> is now <white>{price}</white> <gray>({condition} {target})</gray>."
   title: "<gradient:#7E22CE:#D8B4FE><bold>ᴘʀɪᴄᴇ ᴀʟᴇʀᴛ</bold></gradient>"
   subtitle: "<white>{symbol} • {price}</white>"
-  create-instruction: "<gray>Create an alert with</gray> <white>/market alerts add {symbol} &lt;above|below|percent_up|percent_down|daily_gain|daily_loss&gt; &lt;target&gt;</white><gray>.</gray>"
+  create-instruction: "<gray>Create an alert with</gray> <white>/market alerts add {symbol} \\<above|below|percent_up|percent_down|daily_gain|daily_loss> \\<target></white><gray>.</gray>"
 
 chart:
   header: "<gradient:#7E22CE:#D8B4FE><bold>ᴘʀɪᴄᴇ ʜɪsᴛᴏʀʏ — {symbol}</bold></gradient> <gray>({period})</gray>"
@@ -93,13 +93,13 @@ help:
   player:
     - "<gradient:#7E22CE:#D8B4FE><bold>ᴡɪʟᴅᴍᴀʀᴇ ᴍᴀʀᴋᴇᴛ</bold></gradient>"
     - "<gray>/market</gray> <white>Open the market menu.</white>"
-    - "<gray>/market quote &lt;symbol&gt;</gray> <white>View the latest quote.</white>"
-    - "<gray>/market buy &lt;symbol&gt; &lt;amount&gt;</gray> <white>Buy a virtual asset.</white>"
-    - "<gray>/market sell &lt;symbol&gt; &lt;amount&gt;</gray> <white>Sell a virtual asset.</white>"
+    - "<gray>/market quote \\<symbol></gray> <white>View the latest quote.</white>"
+    - "<gray>/market buy \\<symbol> \\<amount></gray> <white>Buy a virtual asset.</white>"
+    - "<gray>/market sell \\<symbol> \\<amount></gray> <white>Sell a virtual asset.</white>"
     - "<gray>/market portfolio</gray> <white>Open your portfolio.</white>"
-    - "<gray>/market watchlist [add|remove] &lt;symbol&gt;</gray> <white>Manage your watchlist.</white>"
-    - "<gray>/market alerts add &lt;symbol&gt; &lt;above|below|percent_up|percent_down&gt; &lt;target&gt;</gray>"
-    - "<gray>/market history &lt;symbol&gt; [1d|7d|30d|90d|1y]</gray>"
+    - "<gray>/market watchlist [add|remove] \\<symbol></gray> <white>Manage your watchlist.</white>"
+    - "<gray>/market alerts add \\<symbol> \\<above|below|percent_up|percent_down> \\<target></gray>"
+    - "<gray>/market history \\<symbol> [1d|7d|30d|90d|1y]</gray>"
   admin:
     - "<gradient:#7E22CE:#D8B4FE><bold>WildMare Market Admin</bold></gradient>"
     - "<gray>/marketadmin reload</gray>"
@@ -107,9 +107,9 @@ help:
     - "<gray>/marketadmin status</gray>"
     - "<gray>/marketadmin cache|clearcache</gray>"
     - "<gray>/marketadmin provider [name]</gray>"
-    - "<gray>/marketadmin enable|disable &lt;symbol&gt;</gray>"
-    - "<gray>/marketadmin addasset|removeasset &lt;symbol&gt;</gray>"
-    - "<gray>/marketadmin resetportfolio &lt;player&gt;</gray>"
-    - "<gray>/marketadmin setholding|giveasset|takeasset &lt;player&gt; &lt;symbol&gt; &lt;amount&gt;</gray>"
-    - "<gray>/marketadmin transaction &lt;player&gt;</gray>"
+    - "<gray>/marketadmin enable|disable \\<symbol></gray>"
+    - "<gray>/marketadmin addasset|removeasset \\<symbol></gray>"
+    - "<gray>/marketadmin resetportfolio \\<player></gray>"
+    - "<gray>/marketadmin setholding|giveasset|takeasset \\<player> \\<symbol> \\<amount></gray>"
+    - "<gray>/marketadmin transaction \\<player></gray>"
     - "<gray>/marketadmin debug [on|off]</gray>"
